```
根据实际的文件目录，采用深度优先搜索指定的目录．并在合适的位置
安置$guard-file文件．　内容为$timestamp.

  -clean-guard
    	clean the guard files and exit
  -count int
    	number of guard file you wish to set. The realy guard files is not exactly same as the number (default 100)
  -debug
    	
  -guard-file string
    	the guard file name (default "__GUARD__")
  -guard-index string
    	the guard index name under root directory (default "__GUARD__INDEX__")
  -prefix string
    	it will replace the root directory in guard-index-name (default "http://packages.deepin.com/deepin/unstable")
  -timestamp string
    	the default time stamp (default "1454662994")

```
